<% content_for :title, "Editar producto — Admin" %>

<section class="page-head">
  <div>
    <p class="page-head__eyebrow">Productos</p>
    <h1 class="page-head__title">Editar <%= @product.name %></h1>
  </div>
</section>

<div class="card">
  <%= render "form", product: @product %>
</div>
