<% content_for :title, "Editar promoción — Admin" %>

<section class="page-head">
  <div>
    <p class="page-head__eyebrow">Promociones</p>
    <h1 class="page-head__title">Editar <%= @promotion.name %></h1>
  </div>
</section>

<div class="card">
  <%= render "form", promotion: @promotion %>
</div>
