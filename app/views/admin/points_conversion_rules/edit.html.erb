<% content_for :title, "Editar regla — Admin" %>

<section class="page-head">
  <div>
    <p class="page-head__eyebrow">Reglas</p>
    <h1 class="page-head__title">Editar regla</h1>
  </div>
</section>

<div class="card">
  <%= render "form", rule: @rule %>
</div>
