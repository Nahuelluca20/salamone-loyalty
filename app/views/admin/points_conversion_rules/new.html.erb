<% content_for :title, "Nueva regla — Admin" %>

<section class="page-head">
  <div>
    <p class="page-head__eyebrow">Reglas</p>
    <h1 class="page-head__title">Nueva regla</h1>
  </div>
</section>

<div class="card">
  <%= render "form", rule: @rule %>
</div>
